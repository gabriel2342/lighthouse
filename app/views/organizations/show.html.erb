<p style="color: green"><%= notice %></p>
<div class="container">
  <%= render @organization %>

  <div>
    <%= link_to "Edit this organization", edit_organization_path(@organization), class:"btn btn-primary" %> |
    <%= link_to "Back to organizations", organizations_path, class:"btn btn-primary" %>

    <%= button_to "Destroy this organization", @organization, method: :delete, class:"btn btn-primary"%>
  </div>
</div>